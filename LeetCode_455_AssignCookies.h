#pragma once
#include "DataStructure.h"
#include <vector>
using namespace std;
class LeetCode_455_AssignCookies
{
public:
	int findContentChildren(vector<int>& g, vector<int>& s);

	bool selftDefinedSort(int& n1,int& n2);
};